import { DynamicEmailDirective } from './dynamic-email.directive';

describe('DynamicEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicEmailDirective();
    expect(directive).toBeTruthy();
  });
});
